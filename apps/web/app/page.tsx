export default function HomePage() {
  return (
    <main style={{ padding: 24, fontFamily: 'sans-serif' }}>
      <h1>Next.js API Backend</h1>
      <p>Use the /api endpoints for auth and tasks.</p>
    </main>
  );
}
